# WHOOP OAuth Configuration
WHOOP_CLIENT_ID=your_client_id_here
WHOOP_CLIENT_SECRET=your_client_secret_here

# Optional: Custom token file location (defaults to ~/.whoop-mcp/tokens.json)
# WHOOP_TOKEN_FILE=/path/to/custom/tokens.json